name: dev_build

on:
  push:
    branches:
      - master

env:
  IMAGE_NAME: q-n-a
  IMAGE_TAG: master
  DEPLOY_REPO: dev-deploy

jobs:
  build:
    name: Build Docker Image
    strategy:
      matrix:
        platform: [linux/amd64, linux/arm64]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          platforms: ${{ matrix.platform }}
          tags: ghcr.io/q-n-a-dev-team/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}

  scan:
    name: Scan Image Vulnerability
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Pull docker image
        run: docker pull ghcr.io/q-n-a-dev-team/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}
      - name: Container image scan
        uses: Azure/container-scan@v0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          image-name: ghcr.io/q-n-a-dev-team/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}

  deploy:
    name: Create PR to dev-deploy repo
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: Setting up repository
        uses: actions/checkout@v2
        with:
          repository: Q-n-A-dev-team/${{ env.DEPLOY_REPO }}
          token: ${{ secrets.DEPLOY_PAT }}
          persist-credentials: false
          fetch-depth: 0
      - name: Setting up git
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "Q'n'A-backend"
      - name: Make log file
        run: |
          mkdir -p deploy-log/backend
          echo "Deployment of backend-${{ github.sha }}" >> deploy-log/backend/${{ github.sha }}.txt
      - name: Commit
        run: |
          git checkout -b backend-${{ github.sha }}
          git add .
          git commit -m "Requesting deployment of backend-${{ github.sha }}"
      - name: Push
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.DEPLOY_PAT }}
          branch: backend-${{ github.sha }}
          repository: Q-n-A-dev-team/${{ env.DEPLOY_REPO }}
      - name: Create PR
        env:
          GH_TOKEN: ${{ secrets.DEPLOY_PAT }}
        run: gh pr create -R Q-n-A-dev-team/${{ env.DEPLOY_REPO }} -H backend-${{ github.sha }} -t "Requesting deployment of backend" -b "On ${{ github.sha }}"
